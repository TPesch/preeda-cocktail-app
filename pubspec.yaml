name: preeda_cocktail_app
description: "A new Flutter project."
publish_to: "none" # Prevent accidental publication

version: 1.0.0+1

environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.10.0
  flutter_lints: ^3.0.0

flutter_icons:
  android: true
  ios: true
  image_path: "lib/images/Favicon.png" # Path to your app icon

flutter:
  uses-material-design: true

  assets:
    - lib/images/Preeda_Restaurant_Berlin_Logo_200.png
  